# Design 008 - Operations Workbench

This static redesign keeps WordPress Playground's start flows, saved Playground management, embedded browser, and Site Manager tools in one three-pane workspace.

Open `index.html` directly in a browser. No build step is required.
