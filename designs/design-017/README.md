# Design 017 - Playground Project Document

This exploration treats WordPress Playground as a project document for theme authors. A left outline navigates full-height document sections for the active site, creation sources, save state, saved projects, runtime settings, files, blueprints, database tools, logs, and gallery browsing.

Open `index.html` directly in a browser. No build step is required.
