# Design 006 - Playground Command Lens

This exploration makes the command palette the primary information architecture for WordPress Playground. The first viewport is an active launcher over the WordPress frame, with saved Playgrounds, grouped command results, and a contextual inspector visible at once.

Open `index.html` directly or through GitHub Pages.
