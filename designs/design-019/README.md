# Design 019 - Playground Command File

This exploration makes the Blueprint authoring surface the first viewport. It avoids the persistent multi-pane operations console used by earlier explorations and instead uses a compact expert toolbar, structured command menus, and a single vertical stack for progressive disclosure.

Open `index.html` directly in a browser.
